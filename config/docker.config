// To use docker, use nextflow run -profile munin,docker
profiles {
    docker {
        docker {
            enabled      = true
            mountFlags   = 'z'
            fixOwnership = true
        }
    }
}
